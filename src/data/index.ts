// Static data still consumed by the app. The former menu/specials/events/etc.
// fixtures were removed — those pages now load live data from the backend API.
export { businessInfo } from './business';
export { testimonials } from './testimonials';
